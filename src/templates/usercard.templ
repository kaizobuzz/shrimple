package templates

import "fmt"

templ UserCard(logged_in bool, id string, username string) {
	if !logged_in {
		<a class="usercard-class" href="/login.html" id="usercard">
			Not Logged In
		</a>
	} else{
        <a class="usercard-class" href={ templ.URL("/userinfo.html?userid="+fmt.Sprintf("%s", id))}>
            Logged In as {username}
        </a>
    }
}
